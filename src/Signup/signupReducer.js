
const initialState = {
  error:null
};
const signupReducer = function (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
};

export default signupReducer;
